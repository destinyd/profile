[user]
	name = destinyd
	email = destinyd_war@163.com
[alias]
        tree = log --graph --decorate --pretty=oneline --abbrev-commit
        st = status
        ci = commit
        br = branch
        co = checkout
        df = diff
        dc = diff --cached
        lg = log -p
        lol = log --graph --decorate --pretty=oneline --abbrev-commit
        lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
        ls = ls-files
[core]
	editor = vim
        whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = /home/dd/.global_ignore
	;gitproxy = /home/dd/bin/socks5proxywrapper
[color]
  ui = auto
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold
[color "status"]
  added = yellow
  changed = green
  untracked = cyan
[branch "master"]
    remote = origin
    merge = refs/heads/master
[push]
	default = current
;[http]
    ;proxy = socks5://127.0.0.1:8087
